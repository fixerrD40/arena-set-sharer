# Copy to .env. APP_CRYPTO_SECRET must be standard Base64 of at least 32 bytes.
APP_CRYPTO_SECRET=
# App Gmail: SMTP From, and (once registered) ROLE_ROOT for admin grants.
APP_MAIL_USERNAME=
APP_MAIL_PASSWORD=
APP_MAIL_HOST=smtp.gmail.com
APP_MAIL_PORT=587
